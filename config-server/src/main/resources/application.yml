server:
  port: 8888

spring:
  application:
    name: config-server
  threads:
    virtual:
      enabled: true

management:
  tracing:
    sampling:
      probability: 1.0
  otlp:
    metrics:
      export:
        enabled: false
